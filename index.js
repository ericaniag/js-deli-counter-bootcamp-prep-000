function takeANumber(katzDeliLine, name) {
  return "Welcome, " + `${name}` + ". You are number " + parseInt(katzDeliLine.length)+parseInt(1) + " in line."
}

function nowServing(katzDeliLine) {
  
}

function currentLine(katzDeliLine) {
  
}